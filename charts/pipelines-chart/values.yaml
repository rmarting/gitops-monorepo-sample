# Default values for pipelines-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Project Coordinates
project:
  name: gitops-monorepo

application:
  name: sample-app

# Registry

image:
  registry: image-registry.openshift-image-registry.svc:5000
  group: gitops-monorepo-cicd
  name: sample-app

# Nexus Settings

nexus:
  user: admin
  pass: admin123
  url: http://sonatype-nexus-service:8081

# SonarQube Settings

sonarqube:
  user: admin
  pass: admin123
  url: http://sonarqube-sonarqube:9000

# Git Credentials

git:  
  webhook:
    secret: s3cr3t
  secrets: {}
    #- name: github
    #  type: Opaque
    #  labels:
    #    credential.sync.jenkins.openshift.io: "true"
    #  data:
    #    config: YOUT_SEALED_SECRETS_ENCRYPTED_DATA_VALUE
    #    credentials: YOUT_SEALED_SECRETS_ENCRYPTED_DATA_VALUE
