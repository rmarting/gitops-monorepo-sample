# Values to apply in Dev Environment

name: "sample-frontend"
nameOverride: "sample-frontend"

image:
  tag: "1.0.0-2022-03-24-22-36-25"

# Config
config:
  enabled: true
  values:
    backendServiceUrl: http://sample-backend-gitops-monorepo-dev.apps.labs.sandbox893.opentlc.com
    environment: dev

# Route
route:
  secure:
    enabled: false
